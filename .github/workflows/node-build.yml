name: node-build
on:
  push:
    branches:
      - main
    paths:
      - 'api-node/**'

jobs:
  build:
    runs-on: ubuntu-latest
    
    services: 
      redis:
        image: redis
        ports:
          -  6379:6379
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      - name: node Setup version 
        uses: actions/setup-node@v2
        with:
          node-version: '18'
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - name: Generate Environment Variables File for Production
        run: |
          cd api-node
          mkdir env
          cd env
          echo "SECRET_KEY=$SECRET_KEY" >> .env
          echo "TOKEN_PREFIX=$TOKEN_PREFIX" >> .env
          echo "REFRESH_PREFIX=$REFRESH_PREFIX" >> .env
          echo "REDIS_HOST=$REDIS_HOST" >> .env
          echo "REDIS_PORT=$REDIS_PORT" >> .env
          echo "REDIS_USERNAME=$REDIS_USERNAME" >> .env
          echo "REDIS_PASSWORD=$REDIS_PASSWORD " >> .env
          echo "MONGO_URL=$MONGO_URL " >> .env
        env:
          SECRET_KEY: ${{ secrets.SECRET_KEY }}
          TOKEN_PREFIX: ${{ secrets.TOKEN_PREFIX }}
          REFRESH_PREFIX: ${{ secrets.REFRESH_PREFIX }}
          REDIS_HOST: ${{ secrets.REDIS_HOST }}
          REDIS_PORT: ${{ secrets.REDIS_PORT }}
          REDIS_USERNAME: ${{ secrets.REDIS_USERNAME }}
          REDIS_PASSWORD: ${{ secrets.REDIS_PASSWORD }}
          MONGO_URL: ${{ secrets.MONGO_URL }}
      - name: Install Dependencies and Surge
        run: |
          cd api-node
          npm install
      - name: Run Test
        run: |
          cd api-node
          npm run test
#       - name: Build Docker image
#         run: |
#           cd api-node
#           docker build -t ${{ secrets.DOCKER_USERNAME }}/${{ secrets.DOCKER_QUIZ_NODE_IMAGE_NAME }}:${{ github.sha }} \
#             --build-arg SECRET_KEY=$SECRET_KEY \
#             --build-arg TOKEN_PREFIX=$TOKEN_PREFIX \
#             --build-arg REFRESH_PREFIX=$REFRESH_PREFIX \
#             --build-arg REDIS_HOST=$REDIS_HOST \
#             --build-arg REDIS_PORT=$REDIS_PORT \
#             --build-arg REDIS_USERNAME=$REDIS_USERNAME \
#             --build-arg REDIS_PASSWORD=$REDIS_PASSWORD \
#             --build-arg MONGO_URL=$MONGO_URL \
#             .
#       - name: Push Docker image
#         run: |
#           docker images
#           docker push ${{ secrets.DOCKER_USERNAME }}/${{ secrets.DOCKER_QUIZ_NODE_IMAGE_NAME }}:${{ github.sha }}
