{
    "dependencies": {
        "express": "^4.18.2",
        "nodemon": "^2.0.22"
    },
    "scripts": {
        "dev": "nodemon app.ts",
        "deploy": "aws s3 sync . s3://quizstudio --profile=mztalk",
        "build": "tsc -p ."
    },
    "devDependencies": {
        "@types/express": "^4.17.17"
    }
}
